package component;

  /*
        Responsibilities:
        * Provides a componentEnum type for every concretion of component
     */

public enum ComponentEnum {
    TOGGLE,
    VARIABLE,
    CLOCK,
    DEBUG,
    AND,
    OR,
    NOT,
    NAND,
    NOR,
    XOR,
    EQUIVALENCE,
    NIBBLE_DISPLAY
}
